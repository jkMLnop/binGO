global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'bingo-server'

scrape_configs:
  - job_name: 'bingo-server'
    static_configs:
      - targets: ['bingo-server:8080']
    metrics_path: '/metrics'
    scrape_interval: 10s
    scrape_timeout: 5s

