# Grafana Credentials (for docker-compose)
# Copy this file to .env and update with secure values for production use
# For local development, these defaults are acceptable

GRAFANA_USER=admin
GRAFANA_PASSWORD=admin
